# 设计

## 最小栈

``` c++
// 增加一个辅助栈
void push(int x) {
    if (helper_.empty()) helper_.push(x);
    else {
        if (helper_.top() >= x) { // 若新增的小于之前辅助栈的值则入栈
            helper_.push(x);
        }
    }
    stack_.push(x);
}

```